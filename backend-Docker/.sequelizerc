const {resolver} = require('node:path');

module.exports = {
    'config': resolver(__dirname, 'src', 'config', 'database.js'),
    'migrations-path': resolver(__dirname, 'src', 'database', 'migrations'),
    'models-path': resolver(__dirname, 'src', 'models')
}